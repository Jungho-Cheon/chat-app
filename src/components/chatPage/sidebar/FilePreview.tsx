import React from 'react';

import { FilePreviewContainer } from '../../../styles/chatStyles/sidebarStyles/filePreview-styles';

const FilePreview = (): JSX.Element => {
  return <FilePreviewContainer></FilePreviewContainer>;
};

export default FilePreview;
